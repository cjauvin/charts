apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ include "common.fullname" . }}
  labels:
{{ include "common.labels.standard" . | indent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  template:
    metadata:
      name: {{ include "common.fullname" . }}
      labels:
        app: {{ template "common.name" . }}
        release: {{ .Release.Name | quote }}
    spec:
{{- if eq $.Values.storage "google" }}
      volumes:
      - name: credentials-volume
        configMap:
          name: {{ $.Chart.Name }}-{{ $.Release.Name }}-credentials
{{- else if eq $.Values.storage "local" }}
      volumes:
      - name: storage-volume
        emptyDir: {}
{{- end }}
      containers:
      - name: {{ .Chart.Name }}
        image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        args:
        - --port={{ .Values.service.internalPort }}
        - --debug
{{- if eq $.Values.storage "google" }}
        - --storage=google
        - --storage-google-bucket={{ .Values.googleCloudStorageBucket }}
        - --storage-google-prefix={{ .Values.googleCloudStoragePrefix }}
{{- else if eq $.Values.storage "local" }}
        - --storage=local
        - --storage-local-rootdir=/storage
{{- end }}
        ports:
        - containerPort: {{ .Values.service.internalPort }}
        # livenessProbe:
        #   httpGet:
        #     path: /index.yaml
        #     port: {{ .Values.service.internalPort }}
        # readinessProbe:
        #   httpGet:
        #     path: /index.yaml
        #     port: {{ .Values.service.internalPort }}
        resources:
{{ toYaml .Values.resources | indent 10 }}
{{- if eq $.Values.storage "google" }}
        env:
        - name: GOOGLE_APPLICATION_CREDENTIALS
          value: "/etc/credentials/gke-credentials.json"
        volumeMounts:
        - name: credentials-volume
          mountPath: /etc/credentials
{{- else if eq $.Values.storage "local" }}
        volumeMounts:
        - mountPath: /storage
          name: storage-volume
{{- end }}
